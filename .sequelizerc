const { resolve } = require("path");

module.exports = {
  config: resolve(__dirname, "server", "database", "database.js"),
  'migrations-path': resolve(__dirname, 'server', 'database', 'migrations')
};
